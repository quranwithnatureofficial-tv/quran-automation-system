# Config Files Go Here

Examples (to be created during Module 04 - main.py):
- channels.yaml — per-channel settings (name, translation source, language)
- pipeline.yaml — DRY_RUN flag, retry limits, timeouts, render settings

No secrets/API keys in this folder — those go in a gitignored .env file.
